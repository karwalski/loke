Shared governance type definitions. Defines the usecase struct (id, name, risk level, approved models, owner), gatewayrequest and gatewaydecision structs for the governance gateway pipeline, and helper functions to convert sensitivity levels to risk tiers, format risk tier labels, and format policy decision labels. Exports sensitivitytorisk, risklabel, and decisionlabel. Depends on shared.types and std.str.
