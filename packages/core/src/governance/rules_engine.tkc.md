Evaluates compound governance rules (AND/OR/NOT logic) against an evaluation context containing sensitivity, model tier, request count, hour, and scope. Supports rate limiting with SQLite-backed event tracking. Provides default rules for high-sensitivity cloud routing, burst throttling, off-hours premium use, and confidential-external scope denial.
