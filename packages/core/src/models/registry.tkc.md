Model capability registry that catalogues available LLM models (cloud and local) with their quality scores, speed, context windows, and supported tasks. Provides lookup by task type with optional local-only and tier-speed filtering to find the best model for a given workload. Depends on shared.types, std.str, and std.json.
