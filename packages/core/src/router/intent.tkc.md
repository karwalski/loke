Intent classification module that categorises user prompts into task types (code generation, summarisation, classification, NER, embedding, data analysis, chat) using keyword heuristics first, then falling back to an LLM-based classifier via Ollama when confidence is low. Depends on shared.types, core.models.registry, std.str, std.http, std.json, and shared.log.