Latency-aware routing layer that applies time-to-first-token and total-time constraints based on tolerance tier (interactive, considered, background). Interactive requests are restricted to local models only to meet sub-500ms TTFT targets. Depends on std.str, shared.types, core.router.selector, and shared.log.