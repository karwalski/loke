Schema migration definitions module. Returns the ordered list of all SQL migrations (schema_migrations, settings, audit_log, placeholder_maps, sessions tables). No external dependencies beyond toke standard types.
