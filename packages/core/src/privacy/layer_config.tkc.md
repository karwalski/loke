Privacy layer configuration. Defines per-layer settings including enabled state, priority ordering, detection mode (detect-and-mask vs detect-only), confidence threshold, and entity filter. Provides a default three-layer config (regex, NER, Presidio) with most-restrictive consensus strategy, and functions to retrieve individual layers and list only enabled layers. Exports defaultconfig, getlayer, and enabledlayers. Depends on std.str.
