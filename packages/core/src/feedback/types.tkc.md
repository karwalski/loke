Defines core feedback domain types: rating enum (thumbs up/down/neutral), feature area enum (chat, privacy, routing, governance, agent, compression, dashboard, other), feedback entry struct, and feedback summary struct. Provides label conversion functions for persistence. Depends on shared.types and std.str.
