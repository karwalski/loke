# Privacy-filter sidecar dependencies.
#
# The checked-in .venv contains flask/blinker/click and NOT transformers or
# torch, so it cannot run this service. Install these instead:
#
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# Versions are pinned because the service's output feeds measurements, and an
# unpinned dependency makes a measurement unreproducible (story VM1.2). Pin the
# model revision in models.json for the same reason.
transformers==4.44.2
torch==2.4.1
