m=page.moke.insight;
i=http:std.http;
i=tpl:ooke.template;

// MK2 — Insight Lab page handler
// GET /insight → render Insight Lab UI

f=get(req:http.$req):http.$res{
  <tpl.renderfile("templates/insight.tkt"; @());
}
