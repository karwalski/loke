m=page.moke.settings;
i=http:std.http;
i=tpl:ooke.template;

// MK6.3 — Settings page handler
// GET /settings → render settings UI

f=get(req:http.$req):http.$res{
  <tpl.renderfile("templates/settings.tkt"; @());
}
