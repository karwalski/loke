m=page.moke.upload;
i=http:std.http;
i=tpl:ooke.template;

// MK5.1 + MK5.2 — Upload page handler
// GET /upload → render upload UI

f=get(req:http.$req):http.$res{
  <tpl.renderfile("templates/upload.tkt"; @());
}
