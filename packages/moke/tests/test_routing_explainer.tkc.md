Tests moke's routing decision explainer that shows users why a particular model was selected. Verifies routing decision construction with model, provider, reason, sensitivity, and local/cloud flag, sensitivity label mapping (public/internal/confidential/restricted), and human-readable explanation generation for the "Why this model?" UI feature. Depends on `std.test` and `std.str`.
