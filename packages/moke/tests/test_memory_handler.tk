// Copyright 2026 loke contributors
// SPDX-License-Identifier: Apache-2.0

m=packages.moke.tests.testmemoryhandler;
i=t:std.test;
i=str:std.str;

(* Verify page.moke.memory handler structure.
   The handler imports std.http and ooke.template,
   and exposes a get function that renders templates/memory.tkt. *)

t=$handlerinfo{
  modulename:str;
  templatepath:str;
  hasget:bool;
  importshttp:bool;
  importstemplate:bool
};

f=describememoryhandler():$handlerinfo{
  <$handlerinfo{
    modulename:"page.moke.memory";
    templatepath:"templates/memory.tkt";
    hasget:true;
    importshttp:true;
    importstemplate:true
  }
};

f=main():i64{
  let pass=mut.0;
  let fail=mut.0;

  let info=describememoryhandler();

  (* test: module name is correct *)
  if(str.eq(info.modulename;"page.moke.memory")){pass=pass+1}el{fail=fail+1};

  (* test: template path matches expected *)
  if(str.eq(info.templatepath;"templates/memory.tkt")){pass=pass+1}el{fail=fail+1};

  (* test: handler exposes GET function *)
  if(info.hasget){pass=pass+1}el{fail=fail+1};

  (* test: handler imports std.http *)
  if(info.importshttp){pass=pass+1}el{fail=fail+1};

  (* test: handler imports ooke.template *)
  if(info.importstemplate){pass=pass+1}el{fail=fail+1};

  <fail
};
