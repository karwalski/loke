CLI stats dashboard that displays a summary of loke usage metrics including total requests, tokens saved, cost savings in USD, PII entities intercepted, local/cloud request ratio, and cache hit rate. Uses the i18n translator for labels. Exports the run function. Depends on core.metrics.collector, std.str, shared.log, and loke.platform.i18n.translator.
