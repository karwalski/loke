Configuration loading for loke. Defines the $lokeconfig struct with server port, data directory, log level, and provider URLs (Ollama, Presidio, LLMLingua). Loads and parses TOML configuration from a file path with sensible defaults for all fields. Exports the $lokeconfig type and defaultconfig and loadconfig functions. Depends on std.file and shared.types.
