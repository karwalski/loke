MCP-toke entry point. Initialises correlation-ID logging, creates the default server configuration, and starts the MCP server. Exports the main function. Depends on mcptoke.server, shared.log, and std.str.
