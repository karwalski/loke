Savings and metrics API endpoint. GET returns token savings, cost data, per-provider breakdowns, time-series data, and cache statistics for a given period. POST exports metrics in JSON or CSV format.
