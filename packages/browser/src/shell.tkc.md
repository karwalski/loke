Native web-view shell for browser mode. Opens a platform web-view window pointing at the local loke server, with configurable port, title, dimensions, and dev mode. Exports type $shellconfig and functions defaultconfig, onwindowclose, start, and isserverready (which probes the /api/health endpoint). Depends on std.webview, shared.log, std.net, and std.str.
