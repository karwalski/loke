# known-defects.txt — defects the quality gate tolerates, each with the story
# that removes it. Every line is a debt, not an exemption: the file should only
# ever get shorter.
#
# Format: <path>:<pattern-description>  # <story> <why it cannot be fixed yet>
#
# Adding a line requires a story ID. Removing the last line for a given check
# should be followed by deleting that check's allowlist handling.

# Hardcoded home directory in shipped application source. A fresh clone cannot
# find these paths, so the settings file silently resolves nowhere for anyone
# but the original developer. Both need os.getenv("HOME") via a shared
# shared.paths helper.
#
# Cannot be fixed now: both are .tk files, and the toke/ooke toolchain
# migration is on hold pending ooke 3.0.0, so a change to either cannot be
# compiled or verified. TOOLCHAIN.lock is the authority on the criterion:
# toke is gated by commit ancestry (b66bd8e or later, which is met) and ooke
# by a released 3.0.0, which is not. Do not restate version numbers here. pipeline.tk additionally carries an unrelated
# privacy regression under separate repair and must not be edited until that is
# resolved.
packages/browser/pages/api/pipeline.tk   # X8 / toolchain hold
packages/browser/pages/api/settings.tk   # X8 / toolchain hold
