Unit tests for the browser notifications module. Covers store creation, notification create/remove/mark-read/mark-all-read operations, unread counting, dismiss timing by type (info 5000ms, error 8000ms), bell rendering (badge, ARIA attributes), panel rendering (dialog role, unread class), toast rendering (role by severity, dismiss button), and CSS styles (reduced motion support). Depends on browser.notifications.
