Tests the sensitivity classification system that assigns PUBLIC, INTERNAL, CONFIDENTIAL, or RESTRICTED levels to content. Verifies that financial/health/government IDs are classified as RESTRICTED, personal contact info as CONFIDENTIAL, internal references as INTERNAL, and benign content as PUBLIC. Uses stubs mirroring `core.router.sensitivity`. Depends on `std.test` and `std.str`.
