Tests the provider dispatcher that routes LLM requests to the appropriate provider. Verifies routing logic based on available API keys and user preference: anthropic-first when key present, openai fallback, respect for explicit provider preference, and fallback to ollama when no cloud keys are configured. Uses stubs mirroring `core.providers.dispatcher`. Depends on `std.test` and `std.str`.
