Tests time-dependent storage operations: backup filename contains timestamp separators, backup created_at is ISO formatted (not stub), dashboard widget updated_at is non-zero after set_widget, audit logging succeeds with real timestamps, and time.now()/time.format() produce valid output. Depends on core.storage.backup, core.storage.dashboard, core.storage.audit, core.storage.db, and std.time.
