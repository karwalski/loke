Tests the database backup system. Verifies backup filename construction (loke-backup prefix, .db suffix), full path assembly, VACUUM INTO SQL generation, backup creation with input validation (empty dir rejection), restore target path construction, restore operation validation (empty path rejection), and prune stub behaviour. Uses stubs mirroring `core.storage.backup`. Depends on `std.test` and `std.str`.
