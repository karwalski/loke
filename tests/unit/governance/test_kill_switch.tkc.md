Tests the governance kill switch system. Verifies global, per-provider, per-use-case, and per-agent scope activation, engagement with reason and enabled-by metadata, fallback message generation for blocked requests, and state persistence. Covers the kill switch check wired into the request path by GA1.3. Uses stubbed types mirroring `core.governance.kill_switch`. Depends on `std.test` and `std.str`.
