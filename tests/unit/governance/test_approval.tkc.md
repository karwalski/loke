Tests the approval workflow system. Verifies approval requirement logic across all modes (always, never, by_sensitivity, by_provider), correct handling of sensitivity levels (confidential and restricted trigger approval), local provider exemptions (ollama, mlx), dont-ask-again suppression overriding all modes, and diff line type classification. Uses stubs mirroring `core.governance.approval`. Depends on `std.test` and `std.str`.
