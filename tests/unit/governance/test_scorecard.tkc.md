Tests the governance scorecard that calculates privacy, consent, and audit scores. Verifies privacy score calculation (100 minus leak percentage), consent score (granted/required ratio), audit score (logged/total ratio), correct handling of zero-denominator cases, and score clamping to 0-100 range. Uses stubs mirroring `core.governance.scorecard`. Depends on `std.test` and `std.str`.
