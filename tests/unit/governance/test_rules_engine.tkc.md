Tests the governance rules engine that evaluates compound rules against request data. Verifies condition matching on field/operator/value triples, AND/OR logic composition, action assignment (allow, warn, block), severity classification, and rule evaluation against sample request contexts. Uses stubbed types mirroring `core.governance.rules_engine`. Depends on `std.test` and `std.str`.
