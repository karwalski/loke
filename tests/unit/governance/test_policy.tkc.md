Tests the governance policy engine. Verifies risk classification across sensitivity levels (public, internal, confidential, restricted) and cost thresholds, policy evaluation decisions (allow, allow-with-warning, require-approval) for each risk tier, and control selection per risk tier including the restricted local-only-routing addition. Uses stubs mirroring `core.governance.policy`. Depends on `std.test` and `std.str`.
