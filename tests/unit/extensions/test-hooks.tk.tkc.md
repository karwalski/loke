Unit tests for the plugin registry, contract validation, and anonymisation hook (loke.platform.plugin). Tests plugin registration, lookup, unregistration, version compatibility validation, anonymisation processing (email removal), and edge cases like empty name/version registration. Depends on loke.platform.plugin.registry, loke.platform.plugin.contracts, loke.platform.plugin.anonymisation, std.str, and shared.log.
