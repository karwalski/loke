Tests the guardian system prompt that is injected into every LLM call. Verifies the guardian prompt contains all five mandatory rules (personal information protection, placeholder token handling, PII disclosure refusal, prompt injection detection, audit compliance), confirms version string format, and validates that the prompt is non-empty and non-bypassable. Uses stubs mirroring `core.privacy.guardian`. Depends on `std.test` and `std.str`.
