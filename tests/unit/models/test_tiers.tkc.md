Tests the three-tier inference model configuration system. Verifies tier config generation for Interactive (full GPU, mlock enabled), Considered (partial GPU offload), and Background (maximum offload, mmap enabled) tiers, including correct GPU layer counts, context sizes, batch sizes, and memory management flags. Uses stubbed types mirroring `core.models.tiers`. Depends on `std.test` and `std.str`.
