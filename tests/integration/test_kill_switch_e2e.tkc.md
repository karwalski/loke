End-to-end integration tests for the governance kill switch. Tests: initial disengaged state, engaging with reason, reason persistence, manual release, release clears reason, auto-release timestamp set when minutes > 0, zero auto-release when minutes = 0, widget rendering for engaged and disengaged states, and engaged_by field. Depends on std.test, std.str, core.governance.killswitch, core.storage.settings, and core.storage.db.
