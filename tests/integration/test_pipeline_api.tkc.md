Integration test for the `/api/pipeline` endpoint. Verifies pipeline request construction with model, prompt, max tokens, and streaming flag, response parsing with id, model, content, tokens used, and status fields, correct JSON serialisation of the request/response cycle, and error handling for invalid inputs. Uses stubbed types mirroring the pipeline API contract. Depends on `std.test` and `std.str`.
