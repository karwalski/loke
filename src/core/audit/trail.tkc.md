Immutable audit trail for governance and compliance. Records audit events capturing request id, event type, use-case, model, provider, sensitivity, risk tier, policy decision, token counts, cost, and duration. Supports append-only storage with chain hashing for tamper evidence, filtering by event type or date range, and export to JSON and CSV. Exports auditevent and auditstore types. Depends on std.log and std.string.