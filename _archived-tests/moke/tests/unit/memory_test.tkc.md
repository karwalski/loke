Tests for the memory palace subsystem: drawer organisation into wings/halls/rooms, auto-memory creation from Q&A pairs, keyword-based search with stop-word filtering and relevance scoring, AAAK compression (article/preposition stripping), and palace status reporting. Uses std.test with inline anonymous test blocks. Note: uses pre-v3 patterns (anonymous closures, .get() array builders, .filter/.map) that may need future migration.
