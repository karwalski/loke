Tests for the pipeline panel UI state: verifies 9-stage pipeline definition, emitter event lifecycle (start/complete/skip/error), full pipeline run finalisation with duration totals and error propagation, per-stage detail records, human-readable labels and status symbols, and run history with capacity eviction. Depends on core.pipeline.types, core.pipeline.emitter, core.pipeline.history, std.test, std.string.
