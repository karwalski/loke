Comprehensive integration test for the browser chat pipeline. Exercises the full 9-stage pipeline lifecycle (input through output) with mock providers, verifies stage event ordering, PII anonymisation/restoration, guardian injection, timeout/error handling with stage skipping, and clean text passthrough. Depends on core.privacy.*, core.pipeline.*, core.providers.dispatcher, loke.platform.testing.utilities, shared.log.
