Unit tests for CLI integrations command and external integration presets. Tests command parsing for integrations list/add/test subcommands, validates all 10 preset names (pagerduty, datadog, grafana, splunk, elastic, slack, teams, opsgenie, servicenow, webhook), and verifies external integration struct construction. Depends on cli.commands, cli.integrations, core.governance.external, std.string, std.log.